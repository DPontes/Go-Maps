package main

import "fmt"

func main() {
  colors := map[string]string {     // this map will have ´keys´ of type string and `values` of type string
    "red"   : "#FF0000",
    "green" : "#0000FF",
    "blue"  : "#00FF00",
  }

  fmt. Println(colors)
}
